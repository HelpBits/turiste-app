import { StyleSheet } from 'react-native';

export const colors = {
  primary: 'orange',
  secondary: 'red',
};

export const globalStyleSheet = StyleSheet.create({
  title: {
    fontSize: 20,
    marginVertical: 15,
    marginBottom: 15,
  },
});
